<% if user_signed_in? %>
    You are logged in as <%= current_user.username %>
<% end %>

    <h1>Book It Up</h1>

<% if user_signed_in? %>
    <p>Welcome back, <%= current_user.first_name %>!</p>
    <p> Check out your <%= link_to "lists", lists_path %>! </p>
<% else %>
    <p>Welcome! Please <%= link_to "sign up" , new_user_registration_path %> or
    <%= link_to "sign in" , new_user_session_path%> to get started! </p>
<% end %>