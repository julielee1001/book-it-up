<h1> <%= @list.name %></h1>

<p> <%= @list.description %> </p>
<p>Books In "<%= @list.name %>"</p>

<ul>
    <li><%= link_to "Edit List", edit_list_path(@list) %></li>
    <li><%= link_to "Destroy List", list_path(@list), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                    } %></li>

</ul>